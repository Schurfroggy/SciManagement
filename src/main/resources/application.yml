server:
  port: 9000
spring:
  application:
    name: Sci-Management
  datasource:
    url: jdbc:mysql://www.schurfroggy.cn:3306/sci?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
  data:
    redis:
      database: 0
      host:

      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          max-wait: -1
          min-idle: 0